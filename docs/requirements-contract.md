# 기준 계약 문서 · Sparta_AX_Project (스킬)

> 구현(SKILL.md 작성) 전에 합의해 고정한다. 기준이 바뀌면 SKILL.md보다 이 문서를 먼저 갱신하고 Decisionlog에 이유를 남긴다.

## 스킬 이름
- 임시: `skill-wip` (주제 확정 후 `/스킬명`으로 바꿈) `확인 필요`

## 트리거 키워드 (description에 들어갈, 내가 실제로 말할 표현)
- 예: "○○ 만들어줘", "○○ 정리해줘" — `확인 필요`

## 입력 형식
- 사용자가 `/스킬명` 뒤에 넣는 내용 / 자연어 호출 시 전달되는 정보 — `확인 필요`

## 출력 형식
- 스킬이 만들어내는 결과물의 형태(파일·텍스트·표 등) — `확인 필요`

## 절차 (체크리스트 — SKILL.md 본문이 될 단계)
- [ ] 단계 1 — `확인 필요`
- [ ] 단계 2 — `확인 필요`

## 입출력 예시 (1~2개, SKILL.md에 포함)
- 예시 1: 입력 `...` → 출력 `...` `확인 필요`

## 완료 기준
- `/스킬명` 호출 시 예시와 동일한 출력이 재현된다.
- `SKILL.md` + `out/demo/` 실행 결과가 일치한다.

## 안전장치
- 되돌리기 어려운 작업(메일 발송·파일 삭제 등) 포함 여부: `확인 필요`
- 포함 시 자동 실행 차단, `/스킬명` 수동 호출만 허용.
